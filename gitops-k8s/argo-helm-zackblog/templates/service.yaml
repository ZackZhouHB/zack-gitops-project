apiVersion: v1
kind: Service
metadata:
  name: {{ include "zackblog-helm.fullname" . }}
  labels:
    {{- include "zackblog-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zackblog-helm.selectorLabels" . | nindent 4 }}
