apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zackblog-helm.fullname" . }}-test-connection"
  labels:
    {{- include "zackblog-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zackblog-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
