apiVersion: v1
kind: Service
metadata:
  name: openwebui-service
  namespace: default
spec:
  selector:
    app: openwebui
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: LoadBalancer  # Works with: minikube service openwebui-service --url
